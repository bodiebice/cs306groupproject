To run server, use ```python chatserver.py ```

To run client, use ```python chatclient.py username```

to message, type a message.
to update status using UDP, use ```/status```

to quit, use ```/quit```

to generate ascii, use ```/ascii```


